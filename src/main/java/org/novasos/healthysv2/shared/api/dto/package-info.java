/**
 * Shared API contracts.
 *
 * <p>Input DTOs are immutable records suffixed with {@code Request}; output DTOs
 * are immutable records suffixed with {@code Response}. API DTOs never expose
 * JPA entities. Bean Validation belongs on request components and mapping is
 * delegated to MapStruct.</p>
 */
package org.novasos.healthysv2.shared.api.dto;
