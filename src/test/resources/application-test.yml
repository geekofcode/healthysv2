spring:
  jpa:
    hibernate:
      ddl-auto: validate

healthys:
  storage:
    endpoint: http://localhost:9000
    region: us-east-1
    bucket: healthys-test
    access-key: test
    secret-key: test-secret
    path-style-access: true
  livekit:
    url: ws://localhost:7880
    api-key: test-api-key
    api-secret: test-api-secret-at-least-32-characters-long
    token-ttl-minutes: 15
